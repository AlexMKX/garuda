---
# Meta information for wireguard role

galaxy_info:
  author: alexmkx
  description: WireGuard mesh network configuration role with parallel execution
  company: ""
  license: MIT
  min_ansible_version: "2.9"

  platforms:
    - name: Ubuntu
      versions:
        - focal
        - jammy
    - name: Debian
      versions:
        - bullseye
        - bookworm
    - name: EL
      versions:
        - "8"
        - "9"

  galaxy_tags:
    - wireguard
    - vpn
    - networking
    - mesh

dependencies:
  - role: common

# Role supports two linux_apply entrypoints: provision, destroy.
