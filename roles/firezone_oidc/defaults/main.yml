---
# Purpose: Defaults for firezone_oidc role.
# Entry points: Loaded automatically by roles/firezone_oidc.
# Inputs: Flat top-level variables set by linux_apply's apply_ansible_workload.yml
#   wrapper, which publishes each key of the Terraform-rendered payload as a
#   play-scope variable.
# Outputs: Role-facing variable defaults only.
# Side effects: None.

firezone_oidc_dir: /opt/garuda/firezone
firezone_oidc_server_url: ""
firezone_oidc_api_url: ""

# firezone_oidc_providers: (no default — required in provision)
