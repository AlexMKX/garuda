# plugins/modules

Purpose: index of custom Ansible modules in this repository.

## Status

No custom modules are currently active. The `topology_state_ensure` and `role_input_map` modules were removed in commit 4ba6c69 (refactor: remove legacy topology state flow).
