# plugins/filter

Purpose: index of custom Ansible filter plugins in this repository.

## Filters

No custom filter plugins are currently active. Previously deleted:

- `yaml_include.py` (`yaml_add_include`, `yaml_remove_include`) — removed in phase4 along with the compose-fragment patching workflow it supported.
